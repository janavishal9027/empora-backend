# Application
spring.application.name=Empora
server.port=8080

# =============================
# Database - MySQL (Production)
# =============================
spring.datasource.url=jdbc:mysql://localhost:3306/ems
spring.datasource.username=root
spring.datasource.password=Vittu@2322
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect

# JWT
app.jwt.secret=empora_super_secret_key_that_is_at_least_256_bits_long_for_hs256_algorithm
app.jwt.expiration=86400000

# CORS
app.cors.allowed-origins=http://localhost:3000

# Logging - set DEBUG to see auth details
logging.level.com.quickems=DEBUG
logging.level.org.springframework.security=DEBUG
